# pi-configuration

Configuration assets for pi.

## Setup

```bash
git clone git@github.com:westenhoefer/pi-configuration.git ~/projects/pi-configuration
ln -sfn ~/projects/pi-configuration/AGENTS.md ~/.pi/agent/AGENTS.md
ln -sfn ~/projects/pi-configuration/settings.json ~/.pi/agent/settings.json
```
